2 ui/assets.py — Loads, caches, and scales all game sprite assets.
19     """Singleton-style asset loader — call load_all() once after pygame.init()."""
33     # ─── public API ───────────────────────────────────────────────────
46     # ─── tile getters ─────────────────────────────────────────────────
94     # ─── UI background asset getters ──────────────────────────────────
136     # ─── Button getters (sliced from buttons.png) ─────────────────────
143     # ─── Icon getters (sliced from icons.png) ─────────────────────────
151     # ─── Difficulty card getters (sliced from Modes.png) ──────────────
157     # ─── player sprite getters ────────────────────────────────────────
164     # ─── scaled copies (for special sizes like title screen cookie) ───
176     # ─── internal loading ─────────────────────────────────────────────
183         """Scale surface to size×size using smooth scaling."""
190         # The filled_tiles.png is a 10x10 grid of 640x640 — each cell is 64x64
202         # Individual sprite tiles — load and scale to tile_size
230         """Slice the 2×2 delivery_man sprite sheet into 4 directional poses."""
239         # 2×2 layout:
258         # ── Sky background (Frame 1.png — the main background image) ──
264         # ── Home panel ──
269         # ── Logo ──
274         # ── Modes strip ──
288         # ── Scoreboard panel ──
293         # ── Status bar (HUD) ──
298         # ── Grid backgrounds ──
307         # ── Buttons spritesheet (350×719, 6 buttons stacked vertically) ──
325         # ── Icons spritesheet (1251×960, 4 columns × 3 rows) ──
334             # Looking at the image: 4 cols × 3 rows = 12 icons
353         # ── Story and Intro Slide Loading ──
